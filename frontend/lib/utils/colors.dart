import 'package:flutter/material.dart';

class QColors {
  // Warna Utama (Brand Color)
  static const Color primaryPurple = Color(0xFFBB86FC);
  static const Color primaryDark = Color(0xFF3700B3);

  // Background & Surface (Material Dark Theme)
  static const Color background = Color(0xFF121212); // Hitam Deep
  static const Color surface = Color(
    0xFF1E1E1E,
  ); // Hitam Abu (untuk kartu/container)
  static const Color cardGrey = Color(0xFF2C2C2C);

  // Warna Teks
  static const Color textMain = Color(0xFFFFFFFF); // Putih Bersih
  static const Color textSub = Color(
    0xFFB3B3B3,
  ); // Abu-abu (untuk deskripsi/subtitle)

  // Warna Status
  static const Color error = Color(0xFFCF6679);
  static const Color success = Color(0xFF03DAC6);
}
